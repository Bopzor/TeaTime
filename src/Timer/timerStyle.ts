import { CSSProperties } from 'react';

export const timerWrapper: CSSProperties = {
  flex: 3,
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}

export const timeStyle: CSSProperties = {
  fontSize: '4.5em',
  fontFamily: 'monospace',
}

export const controlStyle: CSSProperties = {
  position: 'absolute',
  bottom: '100px',
}